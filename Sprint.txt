BACKLOG

                                                                  - App -
- Tecnologías agregadas (pendiente)
. MongoDB with Robo 3t (replacing sql) X
. Nextjs X
. Typescript X

                                                            - cuentas de usuario -       

                              Persistencia del estado global y reautenticacion del usuario al recargar la pagina

. En el backend, enviar al usuario autenticado un JWT token y un refresh token. (duracion del jwt token: 5-10mins, refresh token: 1-2hs)
. El jwt token se va a guardar en memoria y el refresh token en una cookie http secure.
. Al recargar la pagina, el estado global se reinicia, pero al incio de la aplicacion (usando useEffect en un nivel superior)
  se va a hacer una solicitud al backend para verificar si el refresh token es valido, y asi pedir un nuevo jwt token.
. Si el refresh token es válido se va a enviar un nuevo jwt token que vamos a almacenar en memoria (estado global de redux).
. En el estado global se va a guardar: nombre de usuario, rol, isAUthenticated.

- al estar autenticado, los users tendran acceso al main modal para usarlo libremente, y el navbar muestra su nombre, y opciones
  para acceder al componente Configuración, Logout, Etc...
- para hacer la lista de users fijarse en el curso de node en la parte que hace tracks.js, segun el dijo que puede servir para hacer una lista
- capacidad de hacer click en la vista "Users" en linea/lista de usuarios y ver su contenidos publico
- los perfiles se almacenaran en un endpoint /profiles/AlgunPerfil
- crear rol de admin para darme acceso a cosas de admin que no tienen los que tienen rol de user
- implementar D3 para la grafica que muestra en cada perfil de usuario el almacenamiento usado

                                                                 - Live chat -
- Tener un contador con los usuarios conectados
- Un aviso de cuando un usuario se conecta/desconecta
- Nombre de usuario traido desde la base de datos
- usar IndexedDB para permitir el almacenado de mensajes y recuperación de datos en almacenamiento local

                                                                 - My files -
Permitir a usuarios subir archivos e imagenes
- Permitir usar esta opción solo si el cliente esta logeado como usuario
- usar WebWorkers para la carga pesada de archivos (dedicated-worker)
- Quizas no sea necesario usar web worker para subir mis archivos porque es opcional y se usaria para subir archivos por partes (chunks)
sin bloquear el hilo principal de la web y hacer otras cosas pesadas sin bloquear el hilo principal, pero para la subida de archivos voy a poner
un mensaje que diga subiendo archivos, asi que el usuario no va a interactuar con nada.
En su lugar mejor COMPRIMIR(importante) los archivos en el frontend antes de enviarlos al servidor y que se almacenen ahi
- Opción para hacer publico o privado esta seccion
- Cuando hay muchos archivos (imagenes, etc...) en vez de hacer un dropdown mejor hacer un scroll infinito que vaya cargando    
  de a poco en el componente los elementos
- Reforzar la seguridad de esta sección para que no puedan hackearla si esta puesta como privada

--------------------------------------------------------------------------------------------------------------------------------

- css tip: usar en la clase del div padre para imagenes 

width: min(1000px, 100%);
margin: 0 auto;
columns: 3 300px;
column-gap: 1em;

cada imagen clase:
display: block;
margin-bottom: 1em;
width: 100%;
